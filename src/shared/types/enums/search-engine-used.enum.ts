/**
 * Enum de motores de búsqueda utilizados para la verificación factual.
 */
export enum SearchEngineUsed {
    BRAVE = 'brave_search',
    GOOGLE = 'google_search',
    FALLBACK = 'fallback_search',
}
