/**
 * Enum específico para proveedores de embeddings en el sistema.
 * Actualmente solo se permite OpenAI como proveedor de embeddings.
 */
export enum LlmEmbeddingProvider {
    OPENAI = 'openai',
}
